% git, code

# Count the number of commits between current and target branch
git rev-list --count <branch> ^<target-branch>

$ branch: git rev-parse --symbolic-full-name --abbrev-ref HEAD
$ target-branch: git branch | awk '{print $NF}'
